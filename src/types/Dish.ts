export interface Dish {
  id: string;
  name: string;
  price: string;
  description: string;
  ingredients: string;
  image: string;
}

export interface Beverage {
  name: string;
  price: string;
}
